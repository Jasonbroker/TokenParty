{
  "name": "tokenparty-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @tokenparty/tokenparty dev",
    "dev:dashboard": "pnpm --filter dashboard dev",
    "build": "pnpm -r build",
    "build:publish": "pnpm --filter @tokenparty/tokenparty build:publish",
    "prepublishOnly": "pnpm run build:publish"
  }
}
